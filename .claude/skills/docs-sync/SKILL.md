---
name: docs-sync
description: Synchronize NEWXAU documentation and AI memory with verified repository facts.
allowed-tools: Read, Edit, Write, Glob, Grep, Bash(git *), Bash(node *)
---

# Documentation Sync

Follow `.agents/skills/docs-sync/SKILL.md`. Read the memory index, current state, handoff, and changed source/tests. Keep the task log append-only, update only facts that changed, and run the checker.

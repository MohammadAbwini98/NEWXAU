---
name: feature-implementation
description: Implement a scoped NEWXAU feature with existing architecture, safeguards, tests, and memory sync.
allowed-tools: Read, Edit, Write, Glob, Grep, Bash(git *), Bash(python *), Bash(node *)
---

# Feature Implementation

Follow `.agents/skills/feature-implementation/SKILL.md`. Read the memory index, current state, handoff, architecture, rules, and task-relevant domain docs. Run focused verification, update relevant memory, and run the checker.
